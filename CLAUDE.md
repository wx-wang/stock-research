# 外部 Agent 调用本机投资 Agent（headless 委托协议）

本机（DSH Desktop + investment preset）可通过 `dsh-headless` 一次性任务模式执行投资研究。你是外部编码 Agent，负责**拆任务 → 调命令 → 收结果 → 交投资者复核**。

## 命令

```
dsh-headless "<自包含任务>"
```

- 每次调用 = 全新会话，**无上下文记忆**；多步推进靠文件接力（任务里显式引用已有产物路径）。
- 工作目录固定为投资工作区（本目录），研究产物按归档规则落盘。
- 返回：stdout 摘要 + 产物文件路径；退出码 0=完成，1=出错。

## 任务怎么写（必须自包含）

写明三件事：① 研究类型（行业认知 / 板块机会发现 / 个股 9 阶段 / 数据探针）；② 目标与范围；③ 输出文件路径（`研究输出/...` 或 `research_data/...`）。

示例：

```bash
dsh-headless "做板块机会发现：从 AI 算力链（液冷/CPO/超节点/算力租赁/存储）筛 3-5 个候选方向，按 10 步流程输出到 研究输出/行业研究/市场扫描/20260829_算力链机会发现.md"
dsh-headless "读 研究输出/个股研究/英维克/...Stage2.md，继续阶段 3 利润池，输出 Stage3 备忘录"
dsh-headless "用 Tushare 查药明康德(603259.SH) 最近财报营收净利同比，原始数据写 research_data/tmp/"
```

## 规则

- 遵循根目录 `AGENTS.md`（证据分级、研究闸门、结论标记、不自动交易、不虚构仓位）——**不得要求投资 Agent 绕过**；
- 调用结果**必须交投资者复核**后才能采信；报告结论标记（事实/推断/待验证）要原样保留；
- 完整接入说明见 `研究输出/方法论/20260829_其他Agent接入说明.md`。
